# Orbis System

CRM для консалтинговых агентств, которые отправляют студентов на учёбу в Корею.

Один арендатор — одно агентство со своим поддоменом: `agencyx.orbisystem.us`.
Внутри: база студентов, воронка заявок, досье документов, задачи, дедлайны и
каталог корейских вузов с подбором под портфолио конкретного студента.

Текущий этап — **фасад и несущие конструкции**: архитектура, доменная модель,
роли, мультиарендность и весь интерфейс на демо-данных. Реальные данные вузов
(официальные страницы + файлы admission guideline) подключаются на этапе 2,
структура под них уже заложена.

## Запуск

```bash
npm install
npm run dev      # http://localhost:3000
```

Проверки: `npm run typecheck`, `npm run build`.

### Проверить мультиарендность локально

Поддомены работают и на `localhost`:

```
http://seoulway.localhost:3000     → Seoul Way Education (Ташкент, 2 филиала)
http://agencyx.localhost:3000      → Agency X (Алматы)
http://hanbridge.localhost:3000    → Hanbridge Consulting (Бишкек)
```

Без поддомена арендатор берётся из cookie — переключатель в меню пользователя
справа сверху. Там же переключение сотрудника: интерфейс, объём данных и набор
разделов меняются по роли.

## Разделы

| Раздел | Что делает |
|---|---|
| Дашборд | Сводка дня: воронка, дедлайны недели, задачи, лента агентства |
| Студенты | База профилей: TOPIK, IELTS, GPA, бюджет, города, направления |
| Заявки | Канбан-воронка от первого звонка до вылета |
| Документы | Папка-досье на каждого студента, статусы, апостиль, сроки годности |
| Задачи | Постановка задач сотрудникам, доска по статусам |
| Дедлайны | Автоматически собираются из заявок, документов и задач |
| Каталог вузов | Вузы и программы + подбор под портфолио студента |
| Финансы | Договоры, оплаты, дебиторка, срез по менеджерам |
| Сотрудники | Карточки, нагрузка, матрица прав |
| Настройки | Домен агентства, брендинг, роли, филиалы, тариф, курс доллара |

## Язык и деньги

Интерфейс двуязычный: русский и узбекский, переключение в шапке. Забытый
перевод — ошибка компиляции, а не пустое место на экране.

Договоры агентства ведутся **в сумах**. Стоимость обучения в корейских вузах
остаётся в долларах (так её публикуют сами вузы), рядом система показывает
пересчёт по курсу агентства: `$7 800 · 101 млн сум`. Подробности —
[docs/MONEY-AND-LANGUAGE.md](docs/MONEY-AND-LANGUAGE.md).

## Документация

- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — слои, структура кода, поток данных
- [docs/DATA-MODEL.md](docs/DATA-MODEL.md) — сущности и связи
- [docs/MULTI-TENANCY.md](docs/MULTI-TENANCY.md) — поддомены, DNS, изоляция данных
- [docs/ROLES.md](docs/ROLES.md) — роли, права, зоны видимости
- [docs/UNIVERSITY-CATALOG.md](docs/UNIVERSITY-CATALOG.md) — каталог и алгоритм подбора
- [docs/MONEY-AND-LANGUAGE.md](docs/MONEY-AND-LANGUAGE.md) — валюта и локализация
- [docs/DESIGN.md](docs/DESIGN.md) — дизайн-система
- [docs/ROADMAP.md](docs/ROADMAP.md) — что дальше

## Стек

Next.js (App Router) · React 19 · TypeScript · Tailwind CSS 4.
Данные — типизированные моки в `src/lib/data`, за теми же интерфейсами позже
встаёт база. Серверные компоненты по умолчанию, клиентские — только там, где
нужны фильтры и переключатели.
